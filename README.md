# mnist-robustness-evaluation
This project evaluates the robustness of neural networks under controlled input distribution shift using the MNIST dataset.  It compares a fully connected network and a convolutional neural network under perturbations including Gaussian noise, rotation, and occlusion.
